version: '3'
services:

#  db:
#    image: postgres:latest
#    restart: always
#    environment:
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=postgres
#    ports:
#      - '5432:5432'
#    volumes:
#      - db:/var/lib/postgresql/data

  contacts-api:
    build:
      context: ./
      dockerfile: Dockerfile-contacts-api
    restart: always
    environment:
      DATABASE_URL: "sqlite+aiosqlite:////src/db/contacts.db"
    ports:
      - '8080:8080'
    volumes:
      - ./db:/src/db
